{
  "name": "swadesai-support",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate": "pnpm --filter @swadesai/api db:generate",
    "db:push": "pnpm --filter @swadesai/api db:push",
    "db:seed": "pnpm --filter @swadesai/api db:seed",
    "db:studio": "pnpm --filter @swadesai/api db:studio"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.3",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18"
  }
}